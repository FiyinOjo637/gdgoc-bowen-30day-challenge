version: "3.8"
services:
  api:
    build:
      context: .
      platforms:
        - linux/amd64
    ports:
      - "8000:8000"
    command: python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload